version: "3.9"

services:
  web:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: python app.py
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
